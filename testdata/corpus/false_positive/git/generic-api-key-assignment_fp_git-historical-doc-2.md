# feature-flags integration guide (historical revision)

To authenticate, set the `api_key` field in your config, for example:

    api_key = "example_placeholder_value_not_real"

This revision predates the switch to OAuth2 client credentials.
